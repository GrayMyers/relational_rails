<h1>Passengers in <%=@vehicle.name%></h1>
<%= link_to("Create Passenger", "/vehicles/#{@vehicle.id}/passengers/new") %>
<ul>
  <% @passengers.each do |passenger| %>
    <li>
      <h3><%= passenger.name %></h3>
      <p>Age: <%= passenger.age %></p>
      <p>Driver: <%= passenger.driver %></p>
    </li>
  <% end %>
</ul>
